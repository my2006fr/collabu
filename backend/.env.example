# Copy this file to .env for local development only.
# NEVER commit .env to git — it contains real secrets.

SECRET_KEY=generate_with_python_c_import_secrets_print_secrets_token_hex_32
JWT_SECRET_KEY=generate_another_one_same_way
ENCRYPTION_KEY=generate_with_python_c_from_cryptography_fernet_import_Fernet_print_Fernet_generate_key_decode

ALLOWED_DOMAIN=myiuc.com
JWT_EXPIRES_DAYS=7

# Local SQLite (dev only):
DATABASE_URL=sqlite:///student_collab.db
# Production PostgreSQL (set this on Render, not here):
# DATABASE_URL=postgresql://user:pass@host/dbname

FRONTEND_URL=http://localhost:5173
UPLOAD_FOLDER=uploads
MAX_UPLOAD_MB=50
FLASK_ENV=development
